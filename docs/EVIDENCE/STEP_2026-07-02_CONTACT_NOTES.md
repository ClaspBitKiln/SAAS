# STEP: Contact Notes (CRM Lite slice 1)

Commit:      pending push
CI:          pending
Production:  pending redeploy
DoD:         POST/GET `/contacts/:id/notes` org-scoped; UI Notes panel on contacts page; e2e isolation 404
Known issues: none (local e2e needs `prisma db push` or CI migrate)
