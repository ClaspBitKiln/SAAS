# STEP: F-014 — local .env + prod e2e junk cleanup

Date:        2026-07-03
PR:          #12 (feat/contact-company-link) — merge condition 1
Operator:    Cursor

## F-014 part A: apps/api/.env → local Postgres

| Check | Result |
|-------|--------|
| `apps/api/.env` DATABASE_URL | `postgresql://saas:saas@localhost:5432/saas` ✓ |
| Prod URL in tracked files | **none** (grep hayabusa/railway.internal/proxy.rlwy — 0) |
| `.env` gitignored | yes (`.gitignore`) |
| Reference | root `.env.example` matches local docker-compose |

Note: docker not installed on this machine; local e2e requires `docker compose up postgres` or local Postgres on :5432.

## F-014 part B: prod DB scan + cleanup

### Inventory BEFORE

| tenants | slug | users |
|---------|------|-------|
| Smoke Co | smoke-1782992706 | smoke+1782992706@example.com |
| Мэджик Металл | company-mr4z3o65 | m1@magicmet.ru |

- `e2e-auth-%` tenants (bootstrapE2eAuth): **0** — RED local run did not persist e2e-auth rows (or already absent)
- contacts: 4 (3 MagicMet + 1 Smoke «Test Contact»)
- companies: 0

### Deleted (smoke junk only)

| tenant_id | slug | counts before |
|-----------|------|---------------|
| 019f22a5-a355-7914-b57e-5f76d2f2f3fc | smoke-1782992706 | sessions=1, credentials=1, contacts=1, memberships=1, orgs=1 |

### Inventory AFTER

| tenants | slug | users |
|---------|------|-------|
| Мэджик Металл | company-mr4z3o65 | m1@magicmet.ru (UNTOUCHED) |

- `e2e-auth-%` remaining: **0**
- `GET /health`: ok

## Merge readiness

- ✅ F-014 condition 1
- ✅ PR #12 CI_GREEN (prior)
- ⏳ Awaiting Founder merge + Claude approve
