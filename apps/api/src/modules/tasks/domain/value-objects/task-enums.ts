export enum TaskTypeEnum {
  CALL = 'CALL',
  MESSAGE = 'MESSAGE',
  MEETING = 'MEETING',
  TODO = 'TODO',
}

export enum TaskStatusEnum {
  OPEN = 'OPEN',
  DONE = 'DONE',
  CANCELLED = 'CANCELLED',
}
